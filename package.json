{
  "name": "navix",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter '@navix/core' build && bun run --filter '@navix/react' build",
    "dev": "concurrently --kill-others --names 'core,react,demo' --prefix-colors 'cyan,magenta,green' 'bun run --cwd packages/core dev' 'bun run --cwd packages/react dev' 'bun run --cwd packages/demo dev'",
    "format": "biome format --write .",
    "lint": "biome lint .",
    "check": "biome check --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.12",
    "concurrently": "^9.1.2",
    "tsup": "^8.5.0",
    "typescript": "^5.8.3"
  }
}
