{
  "name": "navix",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter '@navix/react' build",
    "dev": "bun run --cwd packages/react dev",
    "react-demo": "concurrently --kill-others --names 'react,demo' --prefix-colors 'magenta,green' 'bun run --cwd packages/react dev' 'bun run --cwd packages/demo-react dev'",
    "format": "oxfmt",
    "format:check": "oxfmt --check"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "oxfmt": "^0.46.0",
    "tsup": "^8.5.0",
    "typescript": "^5.8.3"
  }
}
